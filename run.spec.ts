import { dsSetUrl } from '.';
import { expect } from 'chai';
import 'mocha';

describe('set server-url function', () => {

  it('should run', () => {
    const result = "Hello world!"
    expect(result).to.equal('Hello world!');
  });

});